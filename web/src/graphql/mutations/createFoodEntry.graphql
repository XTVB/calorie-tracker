mutation CreateFoodEntry($input: FoodEntryInput!) {
  createFoodEntry(input: $input) {
    errors {
      field
      message
    }
    entry {
      id
      name
      calories
      date
      price
      creatorId
    }
  }
}
